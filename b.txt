curl -I -s -o /dev/null -w "%{http_version}\n" https://api.tavily.com
2


environment:
      # 添加这个环境变量
      - CLICKHOUSE_RUN_AS_ROOT=1

docker-compose run --rm -u root --entrypoint chown clickhouse -R 101:101 /var/lib/clickhouse

- ./enable-disk-management.xml:/etc/clickhouse-server/users.d/enable-disk-management.xml

<!-- enable-disk-management.xml -->
<clickhouse>
    <profiles>
        <!-- 
            这里的 'default' 是指默认的用户配置档案。
            当你用 clickhouse-client 连接而未指定用户时，就使用这个profile。
        -->
        <default>
            <!-- 
                将这个标志设置为1，表示允许执行实验性的磁盘管理命令。
                这就是解决 "NOT_IMPLEMENTED" 错误的关键。
            -->
            <allow_experimental_disk_management_commands>1</allow_experimental_disk_management_commands>
        </default>
    </profiles>
</clickhouse>
